name: Generate Sub-READMEs

on:
  workflow_dispatch: # 允许手动触发
  workflow_run:      # 当更新文件的那个工作流完成后自动运行
    workflows: ["Update File Test"]
    types:
      - completed

permissions:
  contents: write

jobs:
  generate-readmes:
    runs-on: ubuntu-latest
    if: ${{ github.event.workflow_run.conclusion == 'success' || github.event_name == 'workflow_dispatch' }}
    steps:
      - name: 检查代码仓库
        uses: actions/checkout@v4
        with:
          ref: ${{ github.ref }}

      - name: 设置 Python 环境
        uses: actions/setup-python@v4
        with:
          python-version: '3.x'

      - name: 安装依赖
        run: pip install pyyaml

      - name: 运行智能对比生成脚本
        run: |
          python3 -c '
          import os
          import urllib.parse
          import yaml
          import datetime

          # --- 配置区域 ---
          REPO_URL = "https://github.com/${{ github.repository }}/blob/main"
          
          # 文件夹显示名称映射
          DIR_MAP = {
              "Official_Examples": "Mihomo 官方示例 (Official)",
              "General_Config": "通用进阶配置 (General Config)",
              "Smart_Mode": "Smart 模式 / 路由专用 (Smart Mode)",
              "Mobile_Modules": "Android 手机模块 (Mobile Modules)"
          }

          # 忽略的文件
          IGNORE_DIRS = [".git", ".github"]
          IGNORE_FILES = ["README.md", "LICENSE", "release_body.md"]

          # --- 辅助函数 ---

          def safe_get(data, keys, default="N/A"):
              val = data
              try:
                  for key in keys:
                      val = val[key]
                  return val
              except:
                  return default

          def get_file_size(path):
              try:
                  size = os.path.getsize(path)
                  if size < 1024: return f"{size} B"
                  return f"{size/1024:.1f} KB"
              except: return "Unknown"

          def analyze_single_config(file_path):
              """解析单个 YAML 并返回结构化数据"""
              try:
                  with open(file_path, "r", encoding="utf-8") as f:
                      data = yaml.safe_load(f)
                      
                  # 基础指标提取
                  info = {}
                  info["mixed_port"] = safe_get(data, ["mixed-port"], "❌")
                  info["http_port"] = safe_get(data, ["port"], "❌")
                  info["mode"] = safe_get(data, ["mode"], "Rule")
                  info["ipv6"] = "✅" if str(safe_get(data, ["ipv6"])).lower() == "true" else "🚫"
                  info["allow_lan"] = "✅" if str(safe_get(data, ["allow-lan"])).lower() == "true" else "🚫"
                  
                  # 进阶指标
                  info["tun"] = "✅ 开启" if safe_get(data, ["tun", "enable"], False) else "🚫 关闭"
                  
                  # 数量统计
                  groups = data.get("proxy-groups", [])
                  info["group_count"] = len(groups) if groups else 0
                  
                  rules = data.get("rules", [])
                  info["rule_count"] = len(rules) if rules else 0
                  
                  # 策略组详情 (用于生成详细报告)
                  info["groups_raw"] = []
                  for pg in groups:
                      name = pg.get("name", "Unknown")
                      type_ = pg.get("type", "select")
                      icon = "🚀"
                      if "auto" in type_ or "url-test" in type_: icon = "♻️"
                      elif "fallback" in type_: icon = "🔧"
                      elif "load-balance" in type_: icon = "⚖️"
                      elif "select" in type_: icon = "👆"
                      info["groups_raw"].append(f"| {icon} {name} | `{type_}` |")

                  # DNS 详情
                  info["dns_raw"] = []
                  dns = data.get("dns", {})
                  if dns.get("enable", False):
                      for ns in dns.get("nameserver", []):
                          if isinstance(ns, str):
                              provider = "DoT" if "tls://" in ns else "DoH" if "https://" in ns else "UDP"
                              info["dns_raw"].append(f"| {provider} | `{ns}` |")
                  
                  return info
              except Exception as e:
                  # print(f"Parse error {file_path}: {e}")
                  return None

          def generate_comparison_table(configs_data):
              """生成横向对比表格"""
              # 表头
              headers = ["特性 / 文件", "大小 (Size)"] 
              for filename in configs_data.keys():
                  headers.append(f"`{filename}`")
              
              lines = []
              lines.append("| " + " | ".join(headers) + " |")
              lines.append("| :--- | :--- " + "| :--- " * len(configs_data) + "|")
              
              # 定义要对比的行
              rows = [
                  ("mixed_port", "混合端口"),
                  ("mode", "运行模式"),
                  ("tun", "TUN 模式"),
                  ("ipv6", "IPv6"),
                  ("allow_lan", "允许局域网"),
                  ("group_count", "策略组数量"),
                  ("rule_count", "规则条数"),
              ]

              # 填充第一行：文件大小
              size_row = ["**文件大小**", "-"]
              for filename, data in configs_data.items():
                  size_row.append(data["file_size"])
              lines.append("| " + " | ".join(size_row) + " |")

              # 填充其他特性行
              for key, label in rows:
                  row_content = [f"**{label}**", "-"]
                  for filename, data in configs_data.items():
                      val = data.get("parsed", {}).get(key, "N/A")
                      # 美化数字显示
                      if key in ["group_count", "rule_count"]:
                          val = f"**{val}**"
                      row_content.append(str(val))
                  lines.append("| " + " | ".join(row_content) + " |")
              
              return "\n".join(lines)

          def generate_readme(root_path, files):
              rel_path = os.path.relpath(root_path, ".")
              folder_name = os.path.basename(root_path)
              title = DIR_MAP.get(folder_name, folder_name)

              # 1. 扫描并解析所有 YAML
              configs_data = {} # {filename: {file_size, parsed}}
              valid_yaml_files = []
              
              for f in sorted(files):
                  if f.lower().endswith((".yaml", ".yml")) and f not in IGNORE_FILES:
                      full_path = os.path.join(root_path, f)
                      # 尝试解析
                      parsed_info = analyze_single_config(full_path)
                      
                      # 只有包含 proxy-groups 或 rules 的才被认为是完整配置，参与对比
                      if parsed_info and (parsed_info["group_count"] > 0 or parsed_info["rule_count"] > 0):
                          configs_data[f] = {
                              "file_size": get_file_size(full_path),
                              "parsed": parsed_info
                          }
                          valid_yaml_files.append(f)

              # --- 开始构建 Markdown ---
              content = []
              content.append(f"# 📂 {title}\n")
              
              # 导航栏
              depth = rel_path.count(os.sep) + 1
              back_link = "../" * depth + "README.md"
              content.append(f"[🔙 返回主页 (Return to Home)]({back_link})\n")

              content.append("> 🤖 **自动分析报告** | Auto-generated Report\n")
              content.append(f"> 本目录包含 **{len(valid_yaml_files)}** 个主要配置文件。\n")

              # --- 模块 1: 对比表格 (如果有多个配置) ---
              if len(configs_data) > 1:
                  content.append("## ⚔️ 配置横向对比 (Comparison)\n")
                  content.append(generate_comparison_table(configs_data))
                  content.append("\n")
              
              # --- 模块 2: 详细分析 (针对每个文件) ---
              if configs_data:
                  content.append("## 📄 配置文件详解 (Details)\n")
                  for filename, data in configs_data.items():
                      info = data["parsed"]
                      file_url = f"{REPO_URL}/{urllib.parse.quote(rel_path)}/{urllib.parse.quote(filename)}"
                      
                      content.append(f"### 📝 {filename}")
                      content.append(f"- **大小**: {data['file_size']}")
                      content.append(f"- **链接**: [查看源码]({file_url})")
                      
                      # 策略组预览
                      if info["groups_raw"]:
                          content.append(f"\n<details>")
                          content.append(f"<summary><b>🔎 点击查看策略组架构 ({info['group_count']}个)</b></summary>\n")
                          content.append("| 策略组 (Group) | 类型 (Type) |")
                          content.append("| :--- | :--- |")
                          content.extend(info["groups_raw"][:15]) # 限制长度
                          if len(info["groups_raw"]) > 15:
                              content.append(f"| ... | 还有 {len(info['groups_raw'])-15} 个 |")
                          content.append(f"\n</details>\n")
                      
                      # DNS 预览
                      if info["dns_raw"]:
                          content.append(f"\n<details>")
                          content.append(f"<summary><b>🌐 点击查看 DNS 配置</b></summary>\n")
                          content.append("| 类型 | 服务器 |")
                          content.append("| :--- | :--- |")
                          content.extend(info["dns_raw"])
                          content.append(f"\n</details>\n")
                      
                      content.append("\n---\n")

              # --- 模块 3: 所有文件列表 (兜底) ---
              content.append("## 📦 所有文件列表 (File List)\n")
              content.append("| 文件名 | 大小 | 链接 |")
              content.append("| :--- | :--- | :--- |")
              for f in sorted(files):
                  if f in IGNORE_FILES or f.startswith("."): continue
                  f_path = os.path.join(root_path, f)
                  f_url = f"{REPO_URL}/{urllib.parse.quote(rel_path)}/{urllib.parse.quote(f)}"
                  content.append(f"| `{f}` | {get_file_size(f_path)} | [查看]({f_url}) |")

              return "\n".join(content)

          # --- 主执行逻辑 ---
          print("Starting Comparison README generation...")
          for root, dirs, files in os.walk("."):
              dirs[:] = [d for d in dirs if d not in IGNORE_DIRS]
              
              rel_path = os.path.relpath(root, ".")
              if rel_path == ".": continue

              # 只有当目录下有文件时才生成
              if len([f for f in files if f not in IGNORE_FILES]) > 0:
                  readme_txt = generate_readme(root, files)
                  with open(os.path.join(root, "README.md"), "w", encoding="utf-8") as f:
                      f.write(readme_txt)
                  print(f"Generated for: {rel_path}")
          '

      - name: 提交更改
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "41898282+github-actions[bot]@users.noreply.github.com"
          
          git add */**/README.md
          git commit -m "Docs: Auto-generate config comparison tables" || echo "No changes to commit"
          
          git push origin HEAD:${{ github.ref_name }}
name: Generate Sub-READMEs

on:
  workflow_dispatch: # 允许手动触发
  workflow_run:      # 当更新文件的那个工作流完成后自动运行
    workflows: ["Update File Test"]
    types:
      - completed

permissions:
  contents: write

jobs:
  generate-readmes:
    runs-on: ubuntu-latest
    if: ${{ github.event.workflow_run.conclusion == 'success' || github.event_name == 'workflow_dispatch' }}
    steps:
      - name: 检查代码仓库
        uses: actions/checkout@v4
        with:
          ref: ${{ github.ref }}

      - name: 设置 Python 环境
        uses: actions/setup-python@v4
        with:
          python-version: '3.x'

      - name: 安装依赖
        run: pip install pyyaml

      - name: 运行智能对比生成脚本
        run: |
          python3 -c '
          import os
          import urllib.parse
          import yaml
          import datetime

          # --- 配置区域 ---
          REPO_URL = "https://github.com/${{ github.repository }}/blob/main"
          
          # 文件夹显示名称映射
          DIR_MAP = {
              "Official_Examples": "Mihomo 官方示例 (Official)",
              "General_Config": "通用进阶配置 (General Config)",
              "Smart_Mode": "Smart 模式 / 路由专用 (Smart Mode)",
              "Mobile_Modules": "Android 手机模块 (Mobile Modules)"
          }

          # 忽略的文件
          IGNORE_DIRS = [".git", ".github"]
          IGNORE_FILES = ["README.md", "LICENSE", "release_body.md"]

          # --- 辅助函数 ---

          def safe_get(data, keys, default="N/A"):
              val = data
              try:
                  for key in keys:
                      val = val[key]
                  return val
              except:
                  return default

          def get_file_size(path):
              try:
                  size = os.path.getsize(path)
                  if size < 1024: return f"{size} B"
                  return f"{size/1024:.1f} KB"
              except: return "Unknown"

          def analyze_single_config(file_path):
              """解析单个 YAML 并返回结构化数据"""
              try:
                  with open(file_path, "r", encoding="utf-8") as f:
                      data = yaml.safe_load(f)
                      
                  # 基础指标提取
                  info = {}
                  info["mixed_port"] = safe_get(data, ["mixed-port"], "❌")
                  info["http_port"] = safe_get(data, ["port"], "❌")
                  info["mode"] = safe_get(data, ["mode"], "Rule")
                  info["ipv6"] = "✅" if str(safe_get(data, ["ipv6"])).lower() == "true" else "🚫"
                  info["allow_lan"] = "✅" if str(safe_get(data, ["allow-lan"])).lower() == "true" else "🚫"
                  
                  # 进阶指标
                  info["tun"] = "✅ 开启" if safe_get(data, ["tun", "enable"], False) else "🚫 关闭"
                  
                  # 数量统计
                  groups = data.get("proxy-groups", [])
                  info["group_count"] = len(groups) if groups else 0
                  
                  rules = data.get("rules", [])
                  info["rule_count"] = len(rules) if rules else 0
                  
                  # 策略组详情 (用于生成详细报告)
                  info["groups_raw"] = []
                  for pg in groups:
                      name = pg.get("name", "Unknown")
                      type_ = pg.get("type", "select")
                      icon = "🚀"
                      if "auto" in type_ or "url-test" in type_: icon = "♻️"
                      elif "fallback" in type_: icon = "🔧"
                      elif "load-balance" in type_: icon = "⚖️"
                      elif "select" in type_: icon = "👆"
                      info["groups_raw"].append(f"| {icon} {name} | `{type_}` |")

                  # DNS 详情
                  info["dns_raw"] = []
                  dns = data.get("dns", {})
                  if dns.get("enable", False):
                      for ns in dns.get("nameserver", []):
                          if isinstance(ns, str):
                              provider = "DoT" if "tls://" in ns else "DoH" if "https://" in ns else "UDP"
                              info["dns_raw"].append(f"| {provider} | `{ns}` |")
                  
                  return info
              except Exception as e:
                  # print(f"Parse error {file_path}: {e}")
                  return None

          def generate_comparison_table(configs_data):
              """生成横向对比表格"""
              # 表头
              headers = ["特性 / 文件", "大小 (Size)"] 
              for filename in configs_data.keys():
                  headers.append(f"`{filename}`")
              
              lines = []
              lines.append("| " + " | ".join(headers) + " |")
              lines.append("| :--- | :--- " + "| :--- " * len(configs_data) + "|")
              
              # 定义要对比的行
              rows = [
                  ("mixed_port", "混合端口"),
                  ("mode", "运行模式"),
                  ("tun", "TUN 模式"),
                  ("ipv6", "IPv6"),
                  ("allow_lan", "允许局域网"),
                  ("group_count", "策略组数量"),
                  ("rule_count", "规则条数"),
              ]

              # 填充第一行：文件大小
              size_row = ["**文件大小**", "-"]
              for filename, data in configs_data.items():
                  size_row.append(data["file_size"])
              lines.append("| " + " | ".join(size_row) + " |")

              # 填充其他特性行
              for key, label in rows:
                  row_content = [f"**{label}**", "-"]
                  for filename, data in configs_data.items():
                      val = data.get("parsed", {}).get(key, "N/A")
                      # 美化数字显示
                      if key in ["group_count", "rule_count"]:
                          val = f"**{val}**"
                      row_content.append(str(val))
                  lines.append("| " + " | ".join(row_content) + " |")
              
              return "\n".join(lines)

          def generate_readme(root_path, files):
              rel_path = os.path.relpath(root_path, ".")
              folder_name = os.path.basename(root_path)
              title = DIR_MAP.get(folder_name, folder_name)

              # 1. 扫描并解析所有 YAML
              configs_data = {} # {filename: {file_size, parsed}}
              valid_yaml_files = []
              
              for f in sorted(files):
                  if f.lower().endswith((".yaml", ".yml")) and f not in IGNORE_FILES:
                      full_path = os.path.join(root_path, f)
                      # 尝试解析
                      parsed_info = analyze_single_config(full_path)
                      
                      # 只有包含 proxy-groups 或 rules 的才被认为是完整配置，参与对比
                      if parsed_info and (parsed_info["group_count"] > 0 or parsed_info["rule_count"] > 0):
                          configs_data[f] = {
                              "file_size": get_file_size(full_path),
                              "parsed": parsed_info
                          }
                          valid_yaml_files.append(f)

              # --- 开始构建 Markdown ---
              content = []
              content.append(f"# 📂 {title}\n")
              
              # 导航栏
              depth = rel_path.count(os.sep) + 1
              back_link = "../" * depth + "README.md"
              content.append(f"[🔙 返回主页 (Return to Home)]({back_link})\n")

              content.append("> 🤖 **自动分析报告** | Auto-generated Report\n")
              content.append(f"> 本目录包含 **{len(valid_yaml_files)}** 个主要配置文件。\n")

              # --- 模块 1: 对比表格 (如果有多个配置) ---
              if len(configs_data) > 1:
                  content.append("## ⚔️ 配置横向对比 (Comparison)\n")
                  content.append(generate_comparison_table(configs_data))
                  content.append("\n")
              
              # --- 模块 2: 详细分析 (针对每个文件) ---
              if configs_data:
                  content.append("## 📄 配置文件详解 (Details)\n")
                  for filename, data in configs_data.items():
                      info = data["parsed"]
                      file_url = f"{REPO_URL}/{urllib.parse.quote(rel_path)}/{urllib.parse.quote(filename)}"
                      
                      content.append(f"### 📝 {filename}")
                      content.append(f"- **大小**: {data['file_size']}")
                      content.append(f"- **链接**: [查看源码]({file_url})")
                      
                      # 策略组预览
                      if info["groups_raw"]:
                          content.append(f"\n<details>")
                          content.append(f"<summary><b>🔎 点击查看策略组架构 ({info['group_count']}个)</b></summary>\n")
                          content.append("| 策略组 (Group) | 类型 (Type) |")
                          content.append("| :--- | :--- |")
                          content.extend(info["groups_raw"][:15]) # 限制长度
                          if len(info["groups_raw"]) > 15:
                              content.append(f"| ... | 还有 {len(info['groups_raw'])-15} 个 |")
                          content.append(f"\n</details>\n")
                      
                      # DNS 预览
                      if info["dns_raw"]:
                          content.append(f"\n<details>")
                          content.append(f"<summary><b>🌐 点击查看 DNS 配置</b></summary>\n")
                          content.append("| 类型 | 服务器 |")
                          content.append("| :--- | :--- |")
                          content.extend(info["dns_raw"])
                          content.append(f"\n</details>\n")
                      
                      content.append("\n---\n")

              # --- 模块 3: 所有文件列表 (兜底) ---
              content.append("## 📦 所有文件列表 (File List)\n")
              content.append("| 文件名 | 大小 | 链接 |")
              content.append("| :--- | :--- | :--- |")
              for f in sorted(files):
                  if f in IGNORE_FILES or f.startswith("."): continue
                  f_path = os.path.join(root_path, f)
                  f_url = f"{REPO_URL}/{urllib.parse.quote(rel_path)}/{urllib.parse.quote(f)}"
                  content.append(f"| `{f}` | {get_file_size(f_path)} | [查看]({f_url}) |")

              return "\n".join(content)

          # --- 主执行逻辑 ---
          print("Starting Comparison README generation...")
          for root, dirs, files in os.walk("."):
              dirs[:] = [d for d in dirs if d not in IGNORE_DIRS]
              
              rel_path = os.path.relpath(root, ".")
              if rel_path == ".": continue

              # 只有当目录下有文件时才生成
              if len([f for f in files if f not in IGNORE_FILES]) > 0:
                  readme_txt = generate_readme(root, files)
                  with open(os.path.join(root, "README.md"), "w", encoding="utf-8") as f:
                      f.write(readme_txt)
                  print(f"Generated for: {rel_path}")
          '

      - name: 提交更改
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "41898282+github-actions[bot]@users.noreply.github.com"
          
          git add */**/README.md
          git commit -m "Docs: Auto-generate config comparison tables" || echo "No changes to commit"
          
          git push origin HEAD:${{ github.ref_name }}
